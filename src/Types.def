#ifndef TYPE
#define TYPE(Kind, Class)
#endif

TYPE(TYPE_FUNC, FuncType)
TYPE(TYPE_PTR, PtrType)
TYPE(TYPE_ID, IDType)
TYPE(TYPE_STR, StrType)
TYPE(TYPE_INT, IntType)
TYPE(TYPE_STRUCT, StructType)

#undef TYPE
