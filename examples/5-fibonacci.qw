printf: (fmt: i8*, ...) -> void;

fib: (x: i32) -> i32 {
  if x < 2 {
    ret x;
  }
  ret fib(x - 1) + fib(x - 2);
}

main: () -> i32 {
  i: i32 = 0;
  while i <= 10 {
    printf("%d\n", fib(i));
    i = i + 1;
  }
  ret 0;
}
